Activity8
1.
SELECT customer_id, MAX(purchase_amount) AS "Max Amt" FROM orders GROUP BY customer_id ORDER BY "Max Amt";

2.
SELECT salesman_id, order_date, MAX(purchase_amount) AS "Max Amt" FROM orders 
WHERE order_date=To_DATE('2012/08/17', 'YYYY/MM/DD') GROUP BY salesman_id, order_date;

3.
SELECT customer_id, order_date, MAX(purchase_amount) AS "Max Amt" FROM orders
GROUP BY customer_id, order_date
HAVING MAX(purchase_amount) IN(2030, 3450, 5760, 6000);
